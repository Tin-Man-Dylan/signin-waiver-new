import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.makercubewaiver.app',
  appName: 'signin-waiver-new',
  webDir: 'dist/signin-waiver-new',
  bundledWebRuntime: false
};

export default config;
